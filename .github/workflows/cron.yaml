name: Trigger post

on:
  schedule:
    - cron: '1,16,31,46 * * * *'

jobs:
  trigger-function:
    runs-on: ubuntu-latest
    steps:
      - name: HTTP Request Action
        uses: fjogeleit/http-request-action@master
        with:
          url: 'https://trading-straregy.vercel.app/api/larkRobotInteraction'
          method: 'POST'
          data: '{"password": "Btc2023"}'
          contentType: 'application/json'
